info:
  name: findAll
  type: http
  seq: 3

http:
  method: GET
  url: "{{gatewayUrl}}/api/{{apiVersion}}/inventory/buildings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
