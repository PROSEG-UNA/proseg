info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{gatewayUrl}}/api/{{apiVersion}}/inventory/buildings"
  body:
    type: json
    data: |-
      {
        "name": "Estudios Generales",
        "campusId": "c0da221f-f87c-48ba-b4b4-c70004f958da"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
