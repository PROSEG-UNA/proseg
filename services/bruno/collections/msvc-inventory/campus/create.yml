info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{gatewayUrl}}/api/{{apiVersion}}/inventory/campuses"
  body:
    type: json
    data: |-
      {
        "name": "UNA - Sarapiquí"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
