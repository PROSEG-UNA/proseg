info:
  name: getBaseRoles
  type: http
  seq: 1

http:
  method: GET
  url: "{{gatewayUrl}}/api/role/base"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
