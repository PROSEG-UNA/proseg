info:
  name: getAllUsers
  type: http
  seq: 2

http:
  method: GET
  url: "{{gatewayUrl}}/api/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
