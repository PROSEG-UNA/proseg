info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{gatewayUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
          "identifier": "isaacbrenes",
          "password": "admin1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
