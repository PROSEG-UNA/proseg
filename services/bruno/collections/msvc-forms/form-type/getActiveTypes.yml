info:
  name: getActiveTypes
  type: http
  seq: 1

http:
  method: GET
  url: "{{gatewayUrl}}/api/{{apiVersion}}/forms/types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
