info:
  name: getTypeById
  type: http
  seq: 2

http:
  method: GET
  url: "{{gatewayUrl}}/api/{{apiVersion}}/forms/types/{{overtimeFormTypeId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
