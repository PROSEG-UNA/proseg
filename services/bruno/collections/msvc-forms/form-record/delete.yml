info:
  name: delete
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{gatewayUrl}}/api/{{apiVersion}}/forms/{{formRecordId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
