info:
  name: findById
  type: http
  seq: 2

http:
  method: GET
  url: "{{gatewayUrl}}/api/{{apiVersion}}/forms/{{formRecordId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
