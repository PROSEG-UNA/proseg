info:
  name: findAll
  type: http
  seq: 7

http:
  method: GET
  url: "{{gatewayUrl}}/api/{{apiVersion}}/maintenance/companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
