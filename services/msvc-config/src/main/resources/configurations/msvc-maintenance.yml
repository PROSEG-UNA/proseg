server:
  port: 8099

spring:
  application:
    name: msvc-maintenance

eureka:
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:msvc-maintenance}
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://msvc-eureka:8761/eureka}