import MaintenanceRequestDetailPanel from '../request/MaintenanceRequestDetailPanel.jsx';

export default function RegisterDetailPanel({ requestId }) {
    return <MaintenanceRequestDetailPanel requestId={requestId} />;
}
