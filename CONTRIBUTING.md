## Commit Convention

We follow Conventional Commits:

type(scope): description

Examples:
- feat(auth): add login endpoint
- fix(gateway): handle timeout error
- docs(readme): update project documentation